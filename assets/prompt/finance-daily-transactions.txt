DELIVERY MANDATE (read first)
Deliver one `FINANCE_DAILY.txt` pack in this reply. Prefer a real download chip; if attach is impossible, use one marked code fence (same pack body). You cannot write to the human’s disk — never say you saved, wrote, or stored a file there. Gemini/Copilot sandbox ≠ their PC.

Daily transactions — structured extraction

You convert a voice dictation of recent money movements into one `FINANCE_DAILY.txt` pack (preview + labeled CSV section).

FILE DELIVERY PROTOCOL (mandatory) — Gemini and Copilot
Runtime authority: if a DATA OUTPUT CONTRACT from Prompt Manager was prepended to this message, follow that for file vs code. Words alone do not create a file; you have no disk access.
You have no disk access. Words alone do not create a file.

Delivery priority (strict):
1. Best: Download / Download code / file card / attachment named exactly `FINANCE_DAILY.txt` (or `gemini-code-….txt` with the markers below).
2. Fallback (only if attach/download is impossible after one retry): exactly ONE fenced `text` (or unlabeled) block whose entire body is the pack. Start with `===PREVIEW===`. No second copy of CSV outside the fence.
3. Forbidden: claiming a disk/Desktop/Downloads/sandbox/“your computer” save; saying a download exists with no chip; multiple unnamed CSV snippets; markdown tables as the primary deliverable; CSV rows loose in the chat bubble.

After delivery, one short chat line only: point at the download chip, or tell the human to copy the code block and save as `FINANCE_DAILY.txt` on Desktop.

PACK BODY LAYOUT (exact markers — required inside the download file OR the fallback fence):

```
===PREVIEW===
N row(s) · brief coverage / skips
===END_PREVIEW===

===FILE: FINANCE_DAILY.csv===
<pure CSV: header + rows>
===END_FILE===
```

Rules:
- Preview is plain text only (coverage / skips). Importer ignores it.
- The FILE section body is pure CSV only — header first, no nested markdown fences inside the section.

Failed delivery — redo:
- Loose description,amount,type,... rows with neither a download control nor a marked fence
- Multiple fences or unnamed CSV snippets without pack markers
- “File is ready / available to download” with no Download control and no fallback fence
- Markdown tables of transactions as the main output
- “I saved / wrote / stored FINANCE_DAILY.txt on your disk / computer / Desktop / Downloads”
- Any local path (`C:\`, `/mnt`, `Downloads`) presented as where the pack now lives

Pre-send self-check (all must pass):
1. Either a real download UI object named `FINANCE_DAILY.txt` when possible, OR exactly one marked fallback fence (not both as primary dumps).
2. Body contains `===PREVIEW===` … `===END_PREVIEW===` and `===FILE: FINANCE_DAILY.csv===` … `===END_FILE===`.
3. CSV body uses the exact header below.
If attach fails: retry Download code once, then use the fence fallback. Never claim a disk save.
If any check fails, regenerate the pack.

CSV body format (inside the FILE section):
- Pure CSV only: first line is the header row below. No preamble, no `FILE:` line, no markdown, no prose inside the section.
- Ignore prior chat noise about skipped items (e.g. "eraser was skipped") when producing this turn's CSV.
- Use UTF-8.
- Use a comma as the field separator.
- Use a comma as the decimal separator (Brazilian). Quote every amount field.
- Convert any dot decimals (31.24) to comma decimals (31,24).
- Do not invent amounts. Skip items you cannot parse (omit those rows from the CSV; do not invent placeholders).

COLUMNS (header required)
description,amount,type,category_id,subcategory,account_id,card_id,transfer_account_id

RULES
- Do not include a date column. AutoHotkey stamps every imported row with yesterday's date at import time. Do not guess or output dates.
- amount: always positive. Example: "12,50"
- type: expense | income | transfer | card_expense
- Transfers: type=transfer, account_id = source, transfer_account_id = destination. Leave category_id empty for transfers.
- Credit-card purchases: type=card_expense. Set card_id to the card's id (e.g. CARD_MP). Set account_id to the card's linked_account_id from credit_cards.csv (or ACC_MERCADOP if unknown). Leave transfer_account_id empty. Never put an account id in card_id or a card id in transfer_account_id.
- Default account: if the speaker omits the account for expense, income, or card_expense, set account_id=ACC_MERCADOP.
- Match account_id and card_id to primary keys in the attached context files (exact name preferred, ignore accents/case).

CATEGORIZATION
- category_id must be copied exactly from the attached categories.csv. Never invent ids (do not output names like CAT_CUIDADOS_PESSOAIS).
- category_id must be a main category (parent_id empty in categories.csv). If a subcategory fits, put its display name in subcategory (e.g. Hairdresser) and its parent's id in category_id. Never put a subcategory id in category_id.
- Map by meaning, not only exact word matches.
- Examples: haircut/barber → category_id=CAT_BELEZA, subcategory=Hairdresser; shampoo/soap → category_id=CAT_MERCADO, subcategory=Personal care; banana → category_id=CAT_MERCADO, subcategory=Produce.
- Prefer a specific subcategory when one fits. Do not leave category_id blank when a clear fit exists.
- Last resort only (use as little as possible; prefer any real fit): expense or card_expense → CAT_MATERIAI (Misc materials); income → CAT_OUTROS2 (Other).
- After the pack, optional one-line coverage note listing only items that used last resort or still had no good fit and why. Do not replace the pack with that note.
- Do not invent category ids. If something is a poor fit, say so in the chat note after the file.

CONTEXT FILES (attached by Utility Shortcuts → Prompts, or Send dictation [D])
- categories.csv — category and subcategory names with ids
- accounts.csv — account names with ids
- credit_cards.csv — card names with ids and linked accounts
- categories-expenses.ini — semantic expense category canon (what each category/subcategory means)
- categories-income.ini — semantic income category canon

Use categories.csv / accounts.csv / credit_cards.csv for id mapping. Use the INI canons only to choose the right category by meaning. Do not invent ids.

DICTATION
Paste the raw dictation below this line.

Reminder: prefer Download code / file card named `FINANCE_DAILY.txt`; if attach fails, one marked code fence with the same pack. Never say it was saved to disk.
---
Human reminders (for the person sending this prompt — do not include in AI output):

After the AI responds:
- Prefer: Quick Download (Win+Alt+Shift+9) on the download chip, then Finance import
- If only a code fence appeared: copy the pack body, save as `FINANCE_DAILY.txt` on Desktop (overwrite any prior copy; never use updated/corrected suffixes), then Finance import
- No rename required: importer extracts the CSV section from `FINANCE_DAILY.txt` or a marked `gemini-code-….txt` and consolidates variants to the canonical name
