DELIVERY MANDATE (read first)
Deliver one `FINANCE_MONTHLY.txt` pack in this reply. Prefer a real download chip; if attach is impossible, use one marked code fence (same pack body). You cannot write to the human’s disk — never say you saved, wrote, or stored a file there. Gemini/Copilot sandbox ≠ their PC.

Monthly investments and goals — structured extraction

You convert a voice dictation about monthly investment P/L and money moved into goals into one `FINANCE_MONTHLY.txt` pack (preview + labeled CSV section).

FILE DELIVERY PROTOCOL (mandatory) — Gemini and Copilot
Runtime authority: if a DATA OUTPUT CONTRACT from Prompt Manager was prepended to this message, follow that for file vs code. Words alone do not create a file; you have no disk access.
You have no disk access. Words alone do not create a file.

Delivery priority (strict):
1. Best: Download / Download code / file card / attachment named exactly `FINANCE_MONTHLY.txt` (or `gemini-code-….txt` with the markers below).
2. Fallback (only if attach/download is impossible after one retry): exactly ONE fenced `text` (or unlabeled) block whose entire body is the pack. Start with `===PREVIEW===`. No second copy of CSV outside the fence.
3. Forbidden: claiming a disk/Desktop/Downloads/sandbox/“your computer” save; saying a download exists with no chip; multiple unnamed CSV snippets; markdown tables as the primary deliverable; CSV rows loose in the chat bubble.

After delivery, one short chat line only: point at the download chip, or tell the human to copy the code block and save as `FINANCE_MONTHLY.txt` on Desktop.

PACK BODY LAYOUT (exact markers — required inside the download file OR the fallback fence):

```
===PREVIEW===
N row(s) · brief coverage / skips
===END_PREVIEW===

===FILE: FINANCE_MONTHLY.csv===
<pure CSV: header + rows>
===END_FILE===
```

Rules:
- Preview is plain text only (coverage / skips). Importer ignores it.
- The FILE section body is pure CSV only — header first, no nested markdown fences inside the section.

Failed delivery — redo:
- Loose CSV rows with neither a download control nor a marked fence
- Multiple fences or unnamed CSV snippets without pack markers
- “File is ready / available to download” with no Download control and no fallback fence
- Markdown tables as the main output
- “I saved / wrote / stored FINANCE_MONTHLY.txt on your disk / computer / Desktop / Downloads”
- Any local path (`C:\`, `/mnt`, `Downloads`) presented as where the pack now lives

Pre-send self-check (all must pass):
1. Either a real download UI object named `FINANCE_MONTHLY.txt` when possible, OR exactly one marked fallback fence (not both as primary dumps).
2. Body contains `===PREVIEW===` … `===END_PREVIEW===` and `===FILE: FINANCE_MONTHLY.csv===` … `===END_FILE===`.
3. CSV body uses the exact header below.
If attach fails: retry Download code once, then use the fence fallback. Never claim a disk save.
If any check fails, regenerate the pack.

CSV body format (inside the FILE section):
- Pure CSV only: first line is the header row below.
- Use UTF-8.
- Use a comma as the field separator.
- Use a comma as the decimal separator (Brazilian). Quote adjustment_amount.
- Convert any dot decimals to comma decimals.
- Signs: profits and contributions are positive; losses and withdrawals are negative. Keep the sign in the field (example: "+1523,45" or "-500,00").

COLUMNS (header required)
entity_type,entity_id,adjustment_amount,description

RULES
- entity_type: account | goal
- entity_id: primary key from the attached CSVs (accounts.csv id or goals.csv id).
- Match names to ids (ignore accents/case). If unmatched, skip the row and add a note in PREVIEW explaining why (do not invent ids).
- description: short English or Portuguese summary of the spoken reason.
- Do not change categories. Do not create transactions except via these balance adjustments.

CONTEXT FILES (attached by Utility Shortcuts → Prompts)
- accounts.csv — account names with ids (investment balances)
- goals.csv — goal names with ids and amounts

Use only these attached CSVs for id mapping. Do not invent ids.

DICTATION
Paste the raw dictation below this line.

Reminder: prefer Download code / file card named `FINANCE_MONTHLY.txt`; if attach fails, one marked code fence with the same pack. Never say it was saved to disk.
---
Human reminders (for the person sending this prompt — do not include in AI output):

After the AI responds:
- Prefer: Quick Download (Win+Alt+Shift+9) on the download chip, then Finance import
- If only a code fence appeared: copy the pack body, save as `FINANCE_MONTHLY.txt` on Desktop, then Finance import
- No rename required: importer extracts the CSV section from `FINANCE_MONTHLY.txt` or a marked `gemini-code-….txt`
