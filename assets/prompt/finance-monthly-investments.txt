DELIVERY MANDATE (read first)
Deliver exactly one real downloadable text file in this same reply. The human uses Quick Download (clicks a Download control) and Finance import. A chat code snippet is not the file. Do not say a download is available unless a download control exists in this reply. Do not claim a Desktop save.

Monthly investments and goals — structured extraction

You convert a voice dictation about monthly investment P/L and money moved into goals into one `FINANCE_MONTHLY.txt` pack (preview + labeled CSV section).

FILE DELIVERY PROTOCOL (mandatory)
The human has no Desktop or disk write access from chat. Words in the bubble do not create a file.

What counts as delivery (must exist in this same reply): a file chip / attachment / Download / Download code / Export control whose payload is the pack. Preferred name `FINANCE_MONTHLY.txt` (chip/button label must match when the platform allows). Acceptable: a `gemini-code-….txt` download whose body uses the markers below.

What does not count (redo the whole turn):
- Any fenced ```txt / ```csv / ```text block in chat as the primary deliverable — even if it contains the pack markers and looks like `FINANCE_MONTHLY.txt`
- Saying the file is ready, attached, or available to download when this message has no download control
- Canvas / doc / “view file” that cannot be saved as a `.txt` download
- Fake local paths (Desktop, `/mnt`, Downloads)

How: use the product’s file / artifact / attachment API (create file, code-execution download, Gemini file card, Claude artifact with download, Copilot/ChatGPT downloadable file). After the file exists, chat may be a one-line pointer at the chip. Do not put CSV rows only in chat.

Delivery priority (strict):
1. Best: one downloadable artifact named exactly `FINANCE_MONTHLY.txt`.
2. Acceptable: a `gemini-code-….txt` download whose body uses the markers below.
3. Forbidden: chat snippets (named or unnamed) as the primary deliverable; markdown tables as the primary deliverable; phantom “ready to download” claims; fake Desktop saves.

PACK BODY LAYOUT (exact markers — required inside the downloadable file):

```
===PREVIEW===
N row(s) · brief coverage / skips
===END_PREVIEW===

===FILE: FINANCE_MONTHLY.csv===
<pure CSV: header + rows>
===END_FILE===
```

Rules:
- Preview is plain text only (coverage / skips). Importer ignores it.
- The FILE section body is pure CSV only — header first, no markdown fences inside the section.

Failed delivery — redo:
- CSV rows in the chat bubble with no download control
- Pack dumped only as a code snippet (with or without pack markers)
- “File is ready / available to download” (or similar) with no Download control in this reply
- Markdown tables as the main output
- “I saved FINANCE_MONTHLY.txt to your Desktop” (or any local path) without a real downloadable artifact

Pre-send self-check (all must pass):
1. This reply has a real UI download object (chip, attachment, Download / Download code / Export) — not a sentence claiming one exists. Named `FINANCE_MONTHLY.txt` when the platform allows.
2. That file’s body contains `===PREVIEW===` … `===END_PREVIEW===` and `===FILE: FINANCE_MONTHLY.csv===` … `===END_FILE===`.
3. CSV body uses the exact header below.
If the platform cannot attach a file, do not output CSV in chat and do not claim success — retry the file tool once. If still impossible, say so in one sentence (no fake download, no snippet-as-file).
If any check fails, regenerate as one `FINANCE_MONTHLY.txt` pack.

CSV body format (inside the FILE section):
- Pure CSV only: first line is the header row below.
- Use UTF-8.
- Use a comma as the field separator.
- Use a comma as the decimal separator (Brazilian). Quote adjustment_amount.
- Convert any dot decimals to comma decimals.
- Signs: profits and contributions are positive; losses and withdrawals are negative. Keep the sign in the field (example: "+1523,45" or "-500,00").

COLUMNS (header required)
entity_type,entity_id,adjustment_amount,description

RULES
- entity_type: account | goal
- entity_id: primary key from the attached CSVs (accounts.csv id or goals.csv id).
- Match names to ids (ignore accents/case). If unmatched, skip the row and add a note in PREVIEW explaining why (do not invent ids).
- description: short English or Portuguese summary of the spoken reason.
- Do not change categories. Do not create transactions except via these balance adjustments.

CONTEXT FILES (attached by Utility Shortcuts → Prompts)
- accounts.csv — account names with ids (investment balances)
- goals.csv — goal names with ids and amounts

Use only these attached CSVs for id mapping. Do not invent ids.

DICTATION
Paste the raw dictation below this line.

Reminder: deliver `FINANCE_MONTHLY.txt` as a real downloadable pack — never a chat snippet standing in for the file, never a download claim without a control.
---
Human reminders (for the person sending this prompt — do not include in AI output):

After the AI responds:
- Run Quick Download (Win+Alt+Shift+9) or Finance import from the launcher
- No rename required: importer extracts the CSV section from `FINANCE_MONTHLY.txt` or a marked `gemini-code-….txt`
