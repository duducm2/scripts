Improve the resolution of the attached image without changing its content.

PRIMARY GOAL:

1. This is a restoration task, not a soft resize. Reconstruct sharp detail; do not preserve existing blur.
2. Gray halos, feathered edges, compression noise, and smearing around text are artifacts to remove — not style to preserve.
3. The output must look like a clean high-resolution export (vector/PDF quality), not an enlarged low-res screenshot.

Before processing:

1. Check that the blurry or low-resolution image is attached.
2. Treat the attached image as the only source of truth.

Text restoration (critical):

The attached image may be a low-resolution export. Gray or light fringes around letters, muddy stroke edges, and blocky noise in white areas next to text are compression/upscale artifacts — not part of the design.

1. Reconstruct each letterform with clean, high-contrast edges: solid ink inside the stroke, solid background outside, with no gray halo or feathered fringe.
2. Small labels, captions, and body text (e.g. "Measure:", "Direction:") must be as sharp as headings — no softer standard for smaller text.
3. Remove JPEG/block noise and mosquito artifacts in flat areas adjacent to text; backgrounds must be uniform solid color.
4. Do not change wording, font weight, size ratios, alignment, or line breaks. Do not invent, substitute, or "correct" any text.

Flat UI and infographic elements:

1. Solid color fills (white, gray cards, colored icons) must be uniform — no gradient smear, blur, or noise inside fills.
2. Lines, boxes, card borders, and icon strokes must have hard, vector-sharp edges and consistent stroke weight.
3. Do not add glow, shadow, feather, or anti-aliasing fringe against backgrounds.

Allowed changes only:

1. Increase output resolution and pixel density.
2. Restore clean boundaries on text, icons, boxes, lines, and UI elements as described above.
3. Remove blur, noise, gray halos, feathered anti-aliasing, compression artifacts, ringing, and smearing.

Forbidden changes:

1. Do not redesign, reinterpret, or restyle the image.
2. Do not recolor, move, resize, crop, rotate, or rearrange any element.
3. Do not change, add, remove, or rewrite any text, icons, shapes, or objects.
4. Do not "improve" the layout, composition, or visual design in any way.
5. Do not upscale by smooth interpolation that preserves existing blur.
6. Do not add soft halos around text or edges for perceived "smoothness".
7. Do not leave visible compression ringing around high-contrast text.
8. Do not treat gray fringes around black or dark text as intentional design.

Output requirements:

1. Preserve the exact same layout, colors, proportions, typography, icons, and style as the attached image.
2. Default target: upscale to 1920 x 1080 for 16:9 slide images, or 2x the attached image dimensions — whichever yields sharper output.
3. All text and icons must be legible at 100% zoom after upscaling.

Self-check before output:

1. At 100% zoom, every text character has crisp edges with no gray outline or halo.
2. White or solid-color space touching text is clean and uniform — no noise blobs or blocky artifacts.
3. Icon, card, and UI edges are vector-sharp, not smudged or over-smoothed.
4. Layout, colors, and content are faithful to the attached source — only resolution and sharpness improved.

Target resolution override (optional):
[Target width x height, e.g. 3840x2160 — leave blank to use the default above]

Reference reminder:
[Attach the blurry or low-resolution image here before sending]
